# --- CORE LIBS ---
# We let the Airflow 3.1.6 constraints manage the versions of these
numpy
pandas
pysqlite3-binary
email-validator
typing-extensions

# --- API & ORCHESTRATION ---
# Removed the 0.109.1 cap; Airflow 3.x works with the latest FastAPI
fastapi
uvicorn[standard]
pydantic-settings
python-dotenv

# --- LANGCHAIN & LLM ---
# Removed the <0.3.0 caps; Airflow 3.x is compatible with LangChain 0.3+
openai
langchain
langchain-openai
langchain-community
langchain_text_splitters
langchain-chroma

# --- VECTOR DATABASE ---
chromadb

# --- DATA PROCESSING ---
pypdf
sentence-transformers

# --- AIRFLOW & INFRASTRUCTURE ---
# Note: We keep the Airflow version here, but your Dockerfile also handles the install
apache-airflow[amazon,postgres]==3.1.6
apache-airflow-providers-postgres
psycopg2-binary
boto3

# --- TESTING ---
pytest
httpx